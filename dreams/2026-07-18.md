# Dream — 2026-07-18

105 episodes recalled (categories: other, implementation, review)

## Lessons
- Ruby fails on **every Aura-specific knowledge question** (bot username, aura-code version, `:provider` command, credentials location, token optimization) across all 5+ runs — it has no project context or docs access. These always escalate.
- Ruby fails on **nearly all implementation tasks**: bash one-liners, git pre-commit hooks, systemd unit files, Python retry/backoff functions, TypeScript env-file readers. Only one implementation task succeeded on ruby (Python HTTP retry, 2026-07-17T23:25).
- Ruby **can** answer general dev-knowledge questions (HTTP 201, JS closures, `process.execPath`, `git rebase -i`, interface vs type) but **inconsistently** — same question passes in one run, fails in another. No determinism.
- Benchmark dataset lives at `/run/media/dusan/DATA1/Aura_Benchmark/questions/` as `tier1.json` through `tier8.json`. Output target was `~/Desktop/answersclean.md`.
- Escalation fallback chain observed: `granite4.1:3b` (early runs, 05:xx UTC) → `deepseek-chat` (07:xx onward) → `glm-5.2` (latest, 2026-07-18). All three handle ruby's failures competently.
- `deepseek-chat` has zero failures post-escalation across all episodes — every task it picks up is approved.

## Patterns
- The **same ~18-question benchmark suite was run at least 5 times** (14:xx 07-16, 05:xx/07:xx/13:xx/23:xx 07-17). This is an evaluation loop, not organic usage.
- Ruby's success rate on general-knowledge "other" tasks degrades over time within a session block — early runs (07-16T14:xx) ruby succeeds on 6/6 general questions; later runs (07-17T05:xx) ruby fails on nearly identical questions.
- Implementation tasks are a near-total loss for ruby: ~95%+ escalation rate across all runs.
- Response times are wildly inconsistent for ruby on identical questions: "interface vs type" ranges from 12s to 157s; "process.execPath" from 71s to 113s. Suggests instability in the model path, not just capability.
- `granite4.1:3b` was swapped out for `deepseek-chat` mid-day on 07-17 — likely a deliberate config change after observing granite's latency/quality.

## Open threads
- The 1802s benchmark-answering task (2026-07-18T11:50) — did `answersclean.md` get fully written? That's 30 minutes of wall time; needs verification that all 8 tiers completed.
- Root cause of ruby's inconsistency on general knowledge questions is unknown — could be timeout, context, rate-limit, or model flapping. Not diagnosed.
- No data on whether ruby failures are empty responses, errors, refusals, or garbage — the logs only show "failed."

## Tomorrow brief
Ruby is unreliable for implementation and Aura-specific tasks; expect escalation to deepseek-chat or glm-5.2 on anything nontrivial. The benchmark suite at `/run/media/dusan/DATA1/Aura_Benchmark/questions/tier1-8.json` is the active workload — verify `answersclean.md` was fully populated from the 1802s run. Diagnosing why ruby intermittently fails on simple general-knowledge questions (same question passes one hour, fails the next) should be the priority investigation.
