# Question Set 03 — t15-05, t16-01

## t15-05
The `daily_run.sh` script runs `npm run build` before capturing Ruby stats. Why is this step necessary, and what specifically breaks if you skip it?

## t16-01
Two Aura sessions run simultaneously in different terminals on the same machine, both writing to the same session store. What can go wrong, and what's the minimal fix that doesn't require a database?
