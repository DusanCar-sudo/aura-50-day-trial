# Question Set 08 — t17-05, t18-01

## t17-05
Aura's context compaction summarizes the middle of the conversation but keeps the first message and the most recent tail verbatim. Explain why keeping the first message matters specifically for cost, not just quality.

## t18-01
A new agent wants to connect to the Aura kanban server via MCP. It calls `kanban_move_card` but sends a card ID from its own internal state that doesn't exist on the board. What should the MCP tool return, and how should the agent handle this gracefully?
