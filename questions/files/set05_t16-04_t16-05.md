# Question Set 05 — t16-04, t16-05

## t16-04
Ollama's `/api/tags` endpoint is called by `resolveRubyConfig` every time a session starts without an explicit `modelName`. On a machine with 20 Ollama models, what's the performance implication, and how would you cache this?

## t16-05
The systemd timer has `Persistent=true`. The machine is shut down for 3 days during a vacation. Describe exactly what happens when it boots, how many benchmark sessions run, and what the timestamps look like.
