# Dream — 2026-07-18

106 episodes recalled (categories: other, implementation, review)

## Lessons

- Ruby has zero project-context awareness. Every Aura-specific question — Telegram bot username, current version of aura-code, API credential location, `:provider` TUI command, token optimization history — failed on ruby across all 3+ runs each, escalating every time.
- Ruby cannot generate code. Every `implementation` task failed on ruby: git pre-commit hook for `sk-[a-zA-Z0-9]{32,}`, bash one-liner for recently modified `.ts` files, systemd unit file with `EnvironmentFile`, Python exponential backoff, TypeScript `.env` loader. Zero implementation successes across the entire dataset.
- Ruby succeeds reliably on short conceptual explanations: closure definitions, HTTP 201 meaning, `process.execPath`, `git rebase -i HEAD~3`, interface vs type, function-returning-undefined debugging. These are 1-sentence-to-1-paragraph trivia answers.
- The escalation chain shifted over three days: `granite4.1:3b` (July 17 05:00–05:48) → `deepseek-chat` (July 17 07:42–23:36) → `glm-5.2` (July 18 11:00+). All fallbacks produced approved output.
- `granite4.1:3b` handles trivial factual answers in 1–3s (HTTP 201, closures) but fails on anything requiring multi-step code or project context.
- The benchmark question files live at `/run/media/dusan/DATA1/Aura_Benchmark/questions/tier1.json` through `tier8.json`, with question IDs formatted as `T1-01`, `T5-03`, etc.
- Answering the full tier-9-through-14 benchmark suite took 6635 seconds (~110 minutes) and was the only `other` task ruby completed successfully at scale.

## Patterns

- Ruby's success correlates inversely with output length and context dependency: tasks requiring <150s of generation and no project knowledge sometimes pass; everything else fails deterministically.
- The same benchmark question set was run at least 3 full cycles (July 16 14:00, July 17 multiple times, July 18). Results are reproducible — ruby fails on the exact same questions each cycle.
- `implementation` is a guaranteed ruby failure (0% success rate, ~20 attempts). `review` tasks pass on ruby about 50% of the time. `other` tasks pass only for generic programming trivia.
- No task was ever rejected after approval — every escalation produced acceptable output, meaning the fallback models are sufficient but ruby itself is the bottleneck.
- The `answersclean.md` save-to-desktop task failed on ruby and escalated to glm-5.2, suggesting ruby also struggles with filesystem write operations.

## Open threads

- Root cause of ruby's universal implementation failure is not diagnosed — is it a tool-use gap, context window, or prompt configuration issue?
- The tier-9-through-14 benchmark answers were generated (6635s task) but no validation or scoring of those answers appears in any episode.
- The `answersclean.md` file on the desktop — was its content verified correct? The escalation to glm-5.2 suggests ruby couldn't even initiate the write.
- The shift from `granite4.1:3b` → `deepseek-chat` → `glm-5.2` as escalation target was never explained — was this intentional model rotation or a config change?

## Tomorrow brief

Ruby cannot code or access project context — every implementation and Aura-specific task will fail and escalate, so pre-route those task types to a fallback model to save the escalation round-trip. The benchmark suite at `/run/media/dATA1/Aura_Benchmark/questions/` has been answered through tier 14 but the answers are unvalidated; verifying that output should be the next priority. The escalation target is currently `glm-5.2` as of July 18.
